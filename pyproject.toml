[project]
name = "nupgis"
version = "0.1.0"
description = "A Python library for GIS using numpy only."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.2.5",
]

[dependency-groups]
dev = [
    "coverage>=7.8.0",
    "pytest>=8.3.5",
]
